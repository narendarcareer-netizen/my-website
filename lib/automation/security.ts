import "server-only";import{createHash,randomBytes}from"node:crypto";export function stableHash(value:unknown){return createHash("sha256").update(JSON.stringify(value)).digest("hex");}export function approvalToken(){return randomBytes(32).toString("base64url");}export function approvalTokenHash(token:string){return createHash("sha256").update(token).digest("hex");}export const automationTestMode=()=>process.env.AUTOMATION_TEST_MODE!=="false";
