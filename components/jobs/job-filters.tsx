import { Search, SlidersHorizontal } from "lucide-react";

export function JobFilters({ companies, values }: { companies: { id: string; name: string }[]; values: Record<string, string> }) {
  const field = "rounded-xl border bg-white px-3 py-2.5 text-sm text-zinc-700 outline-none focus:border-accent-500 focus:ring-2 focus:ring-accent-100";
  return <form className="card mb-6 grid gap-3 p-4 md:grid-cols-2 xl:grid-cols-8"><label className="relative xl:col-span-2"><span className="sr-only">Search jobs</span><Search className="absolute left-3 top-3 size-4 text-zinc-400" /><input name="q" defaultValue={values.q} className={`${field} w-full pl-9`} placeholder="Title or company" /></label><select name="location" defaultValue={values.location} className={field} aria-label="Filter by location"><option value="">All locations</option><option value="Remote">Remote</option><option value="New York">New York</option><option value="San Francisco">San Francisco</option><option value="London">London</option></select><select name="type" defaultValue={values.type} className={field} aria-label="Filter by employment type"><option value="">All job types</option>{["Full-time", "Part-time", "Contract", "Internship"].map(value => <option key={value}>{value}</option>)}</select><select name="company" defaultValue={values.company} className={field} aria-label="Filter by company"><option value="">All companies</option>{companies.map(company => <option key={company.id} value={company.id}>{company.name}</option>)}</select><select name="minMatch" defaultValue={values.minMatch} className={field} aria-label="Minimum match"><option value="">Any match</option><option value="90">90%+</option><option value="80">80%+</option><option value="70">70%+</option></select><select name="sort" defaultValue={values.sort || "match"} className={field} aria-label="Sort jobs"><option value="match">Best match</option><option value="newest">Newest</option><option value="salary">Salary</option></select><div className="flex gap-2"><select name="status" defaultValue={values.status || "active"} className={`${field} min-w-0 flex-1`} aria-label="Filter by status"><option value="active">Active</option><option value="closed">Closed</option></select><button className="button-primary !px-3 !py-2.5" aria-label="Apply filters"><SlidersHorizontal className="size-4" /></button></div></form>;
}
