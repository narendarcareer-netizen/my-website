import { Bookmark, BriefcaseBusiness, Clock3, MapPin } from "lucide-react";
import type { Job } from "@/types/job";
import { StatusBadge } from "./status-badge";

export function JobCard({ job, compact = false }: { job: Job; compact?: boolean }) {
  return <article className="group rounded-2xl border border-zinc-200 bg-white p-5 transition hover:-translate-y-0.5 hover:border-accent-100 hover:shadow-soft"><div className="flex items-start justify-between gap-4"><div className="flex gap-3"><div className={`grid size-11 shrink-0 place-items-center rounded-xl text-xs font-bold text-white ${job.accent}`}>{job.companyInitials}</div><div><p className="text-xs font-medium text-zinc-500">{job.company}</p><h3 className="mt-0.5 font-semibold tracking-tight text-zinc-900">{job.position}</h3></div></div><button className="rounded-lg p-2 text-zinc-400 transition hover:bg-zinc-50 hover:text-accent-600" aria-label={`Save ${job.position}`}><Bookmark className="size-4" /></button></div><div className="mt-4 flex flex-wrap gap-x-4 gap-y-2 text-xs text-zinc-500"><span className="flex items-center gap-1.5"><MapPin className="size-3.5" />{job.location}</span><span className="flex items-center gap-1.5"><BriefcaseBusiness className="size-3.5" />{job.employmentType}</span><span className="flex items-center gap-1.5"><Clock3 className="size-3.5" />{job.datePosted}</span></div>{!compact && <><p className="mt-4 text-sm font-semibold text-zinc-800">{job.salary}</p><div className="mt-3 flex flex-wrap gap-2">{job.skills.map(skill => <span key={skill} className="rounded-lg bg-zinc-50 px-2.5 py-1 text-xs text-zinc-600">{skill}</span>)}</div></>}<div className="mt-5 flex items-center justify-between border-t pt-4"><StatusBadge status={job.status} /><div className="flex items-center gap-2"><div className="h-1.5 w-16 overflow-hidden rounded-full bg-zinc-100"><div className="h-full rounded-full bg-accent-500" style={{ width: `${job.match}%` }} /></div><span className="text-sm font-bold text-accent-700">{job.match}% match</span></div></div></article>;
}
