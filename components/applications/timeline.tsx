export function ApplicationTimeline({events}:{events:Array<{id:string;event_type:string;event_data:Record<string,unknown>;created_at:string}>}){return <ol className="relative ml-2 border-l border-zinc-200">{events.map(e=><li key={e.id} className="mb-6 ml-5"><span className="absolute -left-1.5 mt-1.5 size-3 rounded-full border-2 border-white bg-accent-500"/><time className="text-xs text-zinc-400">{new Intl.DateTimeFormat("en-US",{dateStyle:"medium",timeStyle:"short"}).format(new Date(e.created_at))}</time><p className="mt-1 text-sm font-semibold">{e.event_type.replaceAll("_"," ").toLowerCase().replace(/^./,c=>c.toUpperCase())}</p>{typeof e.event_data.to==="string"&&<p className="text-xs text-zinc-500">Status: {e.event_data.to.replaceAll("_"," ")}</p>}</li>)}</ol>}
