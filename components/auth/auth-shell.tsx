import Link from "next/link";
import { CheckCircle2 } from "lucide-react";
import { Logo } from "@/components/logo";

export function AuthShell({ title, description, children, footer }: { title: string; description: string; children: React.ReactNode; footer: React.ReactNode }) {
  return <main className="grid min-h-screen lg:grid-cols-[.9fr_1.1fr]"><section className="flex flex-col bg-white px-5 py-7 sm:px-10 lg:px-16"><Logo /><div className="mx-auto flex w-full max-w-md flex-1 flex-col justify-center py-12"><h1 className="text-3xl font-semibold tracking-[-0.035em]">{title}</h1><p className="mt-3 text-sm leading-6 text-zinc-500">{description}</p>{children}<div className="mt-7 text-center text-sm text-zinc-500">{footer}</div></div><Link href="/" className="text-xs font-medium text-zinc-400 hover:text-zinc-700">← Back to JobPilot</Link></section><aside className="relative hidden overflow-hidden bg-ink p-16 text-white lg:flex lg:flex-col lg:justify-center"><div className="absolute -right-40 -top-40 size-96 rounded-full bg-accent-600/30 blur-3xl" /><div className="relative max-w-lg"><span className="rounded-full bg-white/10 px-3 py-1.5 text-xs font-semibold text-zinc-300">Your search, secured</span><h2 className="mt-7 text-4xl font-semibold tracking-[-0.04em]">A private workspace for every step toward your next role.</h2><div className="mt-9 space-y-4">{["Your records are protected per account", "Résumé files stay in private storage", "You control your profile and preferences"].map(item => <p key={item} className="flex items-center gap-3 text-sm text-zinc-300"><CheckCircle2 className="size-5 text-accent-500" />{item}</p>)}</div></div></aside></main>;
}
