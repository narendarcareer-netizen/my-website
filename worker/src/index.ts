import{createServer}from"node:http";import{hostname}from"node:os";import{randomUUID}from"node:crypto";import{createClient}from"@supabase/supabase-js";import{UnrecoverableError,Worker}from"bullmq";import{redis}from"./queues/client";import{queueNames}from"./queues/names";import{scanSource}from"./jobs/scan-source";import{scheduleDueSources}from"./schedulers/source-scheduler";import{config}from"./config";import{processOnboarding}from"./jobs/onboard-sources";import{SourceError}from"../../lib/jobs/platform/http";
const cfg=config(),workerId=randomUUID(),workerDb=createClient(cfg.SUPABASE_URL,cfg.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false}}),startedAt=new Date().toISOString();let heartbeatOk=false;async function heartbeat(){const{error}=await workerDb.from("worker_instances").upsert({id:workerId,hostname:hostname(),version:"worker@0.1.0",started_at:startedAt,last_heartbeat_at:new Date().toISOString(),metadata:{environment:cfg.APP_ENV}},{onConflict:"id"});heartbeatOk=!error}await heartbeat();const heartbeatTimer=setInterval(()=>heartbeat().catch(()=>{heartbeatOk=false}),30_000);
const scanWorker=new Worker(queueNames.scan,async job=>{try{return await scanSource(job.data.sourceId)}catch(error){if(error instanceof SourceError&&!error.retryable)throw new UnrecoverableError(error.message);throw error}},{connection:redis(),concurrency:cfg.WORKER_CONCURRENCY,limiter:{max:10,duration:1000}});scanWorker.on("failed",(job,error)=>console.error(JSON.stringify({event:"worker_job_failed",job_id:job?.id,error_code:error.message.slice(0,100)})));const safeError=(error:unknown)=>error instanceof Error?error.message:(typeof error==="object"&&error!==null&&"message"in error?String(error.message):"UNKNOWN").slice(0,200),tick=()=>Promise.all([scheduleDueSources(),processOnboarding()]).catch(error=>console.error(JSON.stringify({event:"scheduler_failed",error_code:safeError(error)}))),timer=setInterval(tick,60_000);await tick();
const healthServer=createServer(async(request,response)=>{if(request.url!=="/health"){response.writeHead(404).end();return}let queueOk=false;try{queueOk=await redis().ping()==="PONG"}catch{}response.writeHead(heartbeatOk&&queueOk?200:503,{"Content-Type":"application/json","Cache-Control":"no-store"});response.end(JSON.stringify({status:heartbeatOk&&queueOk?"ok":"degraded",queue:queueOk?"ok":"unavailable",heartbeat:heartbeatOk?"ok":"unavailable",version:"worker@0.1.0"}))}).listen(cfg.PORT);console.info(JSON.stringify({event:"worker_started",concurrency:cfg.WORKER_CONCURRENCY,port:cfg.PORT}));async function shutdown(){clearInterval(timer);clearInterval(heartbeatTimer);healthServer.close();await scanWorker.close();await redis().quit();process.exit(0)}process.on("SIGINT",shutdown);process.on("SIGTERM",shutdown);
