import{Queue}from"bullmq";import IORedis from"ioredis";import{config}from"../config";import{queueNames}from"./names";let connection:IORedis|undefined;export function redis(){return connection??=new IORedis(config().REDIS_URL,{maxRetriesPerRequest:null})}export const queues=()=>Object.fromEntries(Object.entries(queueNames).map(([key,name])=>[key,new Queue(name,{connection:redis(),defaultJobOptions:{removeOnComplete:1000,removeOnFail:5000,attempts:3,backoff:{type:"exponential",delay:1000}}})]))as Record<keyof typeof queueNames,Queue>;
