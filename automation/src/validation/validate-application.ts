import{classifyQuestion}from"../security/classify-question";import type{FormState,ValidationResult}from"../types/index";export function validateApplication(form:FormState):ValidationResult{const errors:string[]=[],warnings:string[]=[];if(form.captcha)errors.push("CAPTCHA_REQUIRED");if(form.loginRequired)errors.push("LOGIN_REQUIRED");if(form.mfaRequired)errors.push("MFA_REQUIRED");if(!form.documentVersions.resume)errors.push("DOCUMENT_UPLOAD_FAILED");for(const f of form.fields){const kind=classifyQuestion(f.label,f.purpose);if(f.required&&!f.filled)errors.push(kind==="UNKNOWN"?`UNKNOWN_REQUIRED_FIELD:${f.id}`:`REQUIRED_FIELD_MISSING:${f.id}`);if(f.required&&kind==="SENSITIVE"&&!f.filled)errors.push(`SENSITIVE_INPUT_REQUIRED:${f.id}`);if(f.required&&kind==="LEGAL"&&(!f.filled||!f.approved))errors.push(`LEGAL_CONFIRMATION_REQUIRED:${f.id}`);if(kind==="GENERATED_TEXT"&&f.filled&&!f.approved)errors.push(`GENERATED_TEXT_APPROVAL_REQUIRED:${f.id}`);}errors.push(...form.visibleValidationErrors.map(e=>`VALIDATION_ERROR:${e}`));if(!form.documentVersions.coverLetter)warnings.push("Cover letter version not selected; allowed only when optional.");return{ready:errors.length===0,errors:[...new Set(errors)],warnings,fieldsChecked:form.fields.length};}
