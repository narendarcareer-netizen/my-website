import type{Page}from"@playwright/test";import{detectSubmitControl}from"./submit-control";export class LeverAdapter{constructor(private page:Page){}async fillText(label:string,value:string){await this.page.getByLabel(label,{exact:false}).fill(value);}async upload(label:string,path:string){await this.page.getByLabel(label,{exact:false}).setInputFiles(path);}submitControl(){return detectSubmitControl(this.page);}}
