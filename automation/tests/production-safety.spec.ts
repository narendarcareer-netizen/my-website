import{expect,test}from"@playwright/test";import{readFile}from"node:fs/promises";const root=(path:string)=>readFile(new URL(`../../${path}`,import.meta.url),"utf8");test("controlled submit keeps all production invariants",async()=>{const source=await root("app/api/automation/submit/route.ts");for(const invariant of["AUTOMATED_SUBMISSION","authenticateExtension","validateReview","form_snapshot_hash","APPROVAL_EXPIRED","DUPLICATE_SUBMISSION","automationTestMode","recordUsage"])expect(source).toContain(invariant);expect(source).not.toContain("requestSubmit(")});test("legacy submission endpoint cannot bypass controlled flow",async()=>{const source=await root("app/api/extension/submission/route.ts");expect(source).toContain("LEGACY_SUBMISSION_DISABLED");expect(source).toContain("410")});test("production extension build replaces localhost origins",async()=>{const source=await root("extension/vite.config.ts");expect(source).toContain("JOBPILOT_PRODUCTION_ORIGIN");expect(source).toContain("extension-production")});
