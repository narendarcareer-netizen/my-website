export{};const button=document.querySelector<HTMLButtonElement>("#scan")!;const statusElement=document.querySelector("#status")!;button.onclick=async()=>{const[tab]=await chrome.tabs.query({active:true,currentWindow:true});if(!tab.id)return;try{await chrome.scripting.executeScript({target:{tabId:tab.id},files:["content.js"]});statusElement.textContent="Field review panel opened. Generic pages require individual field approval.";}catch{statusElement.textContent="Chrome did not permit access to this page.";}};
