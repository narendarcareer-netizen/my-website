// @vitest-environment jsdom
import{describe,it,expect,beforeEach}from"vitest";import{detectAts}from"../src/content/detect-ats";import{classify,scanFields}from"../src/content/field-detection";
describe("ATS and field safety",()=>{beforeEach(()=>{document.body.innerHTML=""});it("detects Greenhouse",()=>{document.body.innerHTML='<div id="grnhse_app"></div>';expect(detectAts(document,"https://boards.greenhouse.io/acme/jobs/1").atsType).toBe("GREENHOUSE")});it("detects Lever",()=>{document.body.innerHTML='<div class="lever-job-apply"></div>';expect(detectAts(document,"https://jobs.lever.co/acme/1").atsType).toBe("LEVER")});it("marks demographic and government data sensitive",()=>{for(const label of["Race","Disability status","Veteran status","Social Security number","Criminal history"])expect(classify(label,"text").sensitive).toBe(true)});it("maps safe profile fields",()=>{expect(classify("Email address","email").purpose).toBe("EMAIL");expect(classify("LinkedIn profile","url").purpose).toBe("LINKEDIN")});it("never treats unknown custom text as high confidence",()=>{document.body.innerHTML='<label for="q">Tell us something</label><textarea id="q"></textarea>';expect(scanFields(document)[0].confidence).toBeLessThan(.8)})});
