import { DashboardHeader } from "@/components/dashboard-header";

const options = [{ title: "Weekly job digest", description: "A Monday summary of new matches and pipeline activity." }, { title: "Application reminders", description: "A reminder when an application has had no activity for seven days." }, { title: "Interview preparation", description: "Helpful prompts before your scheduled interviews." }];

export default function SettingsPage() {
  return <><DashboardHeader title="Settings" subtitle="Manage your workspace preferences." /><main className="mx-auto max-w-4xl p-5 sm:p-8"><div className="card"><div className="border-b p-6"><h2 className="font-semibold">Notifications</h2><p className="mt-1 text-sm text-zinc-500">Choose which updates you would like to receive.</p></div><div className="divide-y">{options.map((option, i) => <div key={option.title} className="flex items-center justify-between gap-6 p-6"><div><p className="text-sm font-semibold">{option.title}</p><p className="mt-1 text-xs leading-5 text-zinc-500">{option.description}</p></div><label className="relative inline-flex cursor-pointer items-center"><input type="checkbox" defaultChecked={i < 2} className="peer sr-only" /><span className="h-6 w-11 rounded-full bg-zinc-200 transition after:absolute after:left-1 after:top-1 after:size-4 after:rounded-full after:bg-white after:transition peer-checked:bg-accent-600 peer-checked:after:translate-x-5" /></label></div>)}</div></div><div className="card mt-6 p-6"><h2 className="font-semibold">Local data</h2><p className="mt-2 text-sm leading-6 text-zinc-500">Phase 1 uses demonstration data in your project files. No information is being synced to a server or database.</p></div></main></>;
}
