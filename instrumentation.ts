import * as Sentry from"@sentry/nextjs";export async function register(){if(process.env.NEXT_RUNTIME==="nodejs"&&process.env.SENTRY_DSN)Sentry.init({dsn:process.env.SENTRY_DSN,environment:process.env.APP_ENV??process.env.NODE_ENV,tracesSampleRate:Number(process.env.SENTRY_TRACES_SAMPLE_RATE??"0.05"),sendDefaultPii:false,beforeSend(event){if(event.request){delete event.request.data;delete event.request.cookies;delete event.request.headers}return event}})}export const onRequestError=Sentry.captureRequestError;
